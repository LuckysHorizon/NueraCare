import userProfile from './userProfile';
import onboardingProgress from './onboardingProgress';
import medicalReport from './medicalReport';

export const schemaTypes = [userProfile, onboardingProgress, medicalReport];
